name: Test

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

# will cancel previous workflows triggered by the same event and for the same ref for PRs or same SHA otherwise
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ contains(github.event_name, 'pull_request') && github.event.pull_request.head.ref || github.sha }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  test:
    name: ${{ matrix.os }} - ${{ matrix.py }}
    runs-on: ${{ matrix.os }}
    env:
      motherduck_token: ${{ secrets.HARLEQUIN_MOTHERDUCK_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - MacOs-latest
          - Windows-latest
        py:
          - "3.14"
          - "3.13"
          - "3.12"
          - "3.11"
          - "3.10"
    steps:
      - name: Check out Repo
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Python ${{ matrix.py }}
        id: setup-python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.py }}

      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
        with:
          version: "0.12.5"
          python-version: ${{ matrix.py }}
          enable-cache: true

      - name: Install python dependencies
        run: |
          uv sync --locked --group test

      # Windows runners ship without a timezone database, so the test session
      # downloads one into a shared user data directory. Do it once, up front:
      # xdist workers would otherwise race to download and extract it.
      - name: Install timezone database
        if: matrix.os == 'Windows-latest'
        run: |
          uv run --no-sync python -c "from harlequin.windows_timezone import check_and_install_tzdata; check_and_install_tzdata()"

      - name: Run tests
        run: |
          uv run --no-sync pytest -m "not online" ${{ matrix.os == 'Windows-latest' && '--force-flaky --max-runs=3' || '' }} --snapshot-warn-unused

      - name: Store snapshot report on failure
        uses: actions/upload-artifact@v7  
        if: failure()
        with:
          name: Snapshot Report ${{ matrix.os }} ${{ matrix.py }}
          path: ./snapshot_report.html
